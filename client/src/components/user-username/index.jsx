export default function UserUsername({username}) {
    return (
        <span className="font-normal leading-5 text-[color:var(--color-base-secondary)]">@{username}</span>
    )
}

//<div className="text-[color:var(--color-base-secondary)] font-normal">@{account.username}</div>